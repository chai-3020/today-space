# 你要做的事 · 清单（2026-09-21）

配套文档：`OPTIMIZATION-PLAN-2026-09-21.md`（完整方案与验收清单）

---

## 一、已经做完的（不用你管，供核对）

| 项目 | 状态 |
|---|---|
| 高危 A1–A6 全部修复 | ✅ 代码已改、语法自检通过、24 项逻辑断言全过 |
| 中危 B1–B7 全部修复 | ✅ 同上（B3 的索引部分需要你手工点，见下） |
| 云函数重新部署 | ✅ `recordSession` / `getFocusStats` / `clearDoneTodos` / `updateProfile` 全部 `success: true` |
| 小程序上传 | ✅ 版本 **1.0.1**（100.8 KB），可在 mp.weixin.qq.com → 版本管理看到 |
| 本地 git 提交 | ✅ `7708709`，20 个文件变更（+1070 / −275） |
| 代码同步到微信开发者工具 | ✅ 项目目录就是唯一真源，IDE 已在监听；已重新编译并出预览二维码 |

---

## 二、必须你手工做的（共 4 项，我碰不到）

### 1. 云开发控制台：删掉云端的 `addFocus` 函数

- 位置：微信开发者工具 → 云开发 → 云函数 → `addFocus` → 删除
- 为什么：本地目录已删，它是死代码（没有任何页面调用），而且它按"25 的倍数"校验时长，与现在的自定义时长冲突。开发者工具 CLI **没有** delete 命令，所以只能手点。

### 2. 云开发控制台：建索引（B3 的收尾，直接影响统计速度和资源点消耗）

- 数据库 → `focus_log` → 索引管理 → 新增**组合索引**：`openid`（升序）+ `day`（升序）
- 数据库 → `pomo_sessions` → 索引管理 → 新增**组合索引**：`openid`（升序）+ `startedAt`（降序）

### 3. 云开发控制台：确认 6 个集合的权限（设错会让统计全变 0 或串号）

| 集合 | 权限 | 原因 |
|---|---|---|
| `todos` / `notes` / `countdowns` | **仅创建者可读写** | 页面是客户端直连读写；`countdowns` 的查询没有 openid 过滤，完全靠这条权限隔离 |
| `focus_log` / `pomo_sessions` | **所有用户可读，仅创建者可读写** | 云函数用显式 `openid` 字段查询；本轮已给新数据补 `_openid`，若你确认数据都已带 `_openid`，可进一步收紧为"仅创建者可读写" |
| `users` | 同上 | 昵称档案 |

### 4. 模拟器里点一遍这 5 条（代码层面我验过，运行时得你看）

1. **切后台续跑**：开始 1 分钟专注 → 切聊天/别的 tab 等 30 秒 → 切回来 → 数字应继续走，到点弹完成。
2. **后台跨过结束点**：开始 1 分钟专注 → 切后台待满 90 秒 → 回来应提示完成、记录 +1 分钟（且不含后台那 30 秒）。
3. **放弃记录**：专注 2 分钟后点"重置" → 应弹二次确认；确认后统计页"今日专注 → 放弃次数"变成 1（不再是写死的 0）。
4. **深色模式配色**：切深色 → 番茄钟圆环底圈看得见、统计柱子是主题色；再换主题色（紫/粉），圆环与柱子跟着变。
5. **清除已完成**：勾几条待办 → 点"清除已完成" → 应提示"已清除 N 条"且列表真的空（改之前提示 0 条、列表还在）。

---

## 三、GitHub 同步（当前被网络卡住）

- 现状：git 走本地代理 `127.0.0.1:7890`，但**该端口没有进程监听**；不走代理直连 github.com:443 也超时。
- 目标：新建仓库 `chai-3020/today-space-mini`，把本地 `master`（含本次修复提交）推上去。
- 你要做的：**启动 FlClash**（`D:\FlClash\FlClash.exe`）并确认系统代理生效（端口 7890），然后告诉我一声，我执行：
  1. 验证连通性（`gh api` / `git ls-remote`）
  2. `gh repo create chai-3020/today-space-mini`（私有）
  3. `git remote add origin` + `git push -u origin master`

> 另注：`D:\codex  use\site` 仓库有 **52 个 auto-sync 提交未推送**（代理挂掉后一直在本地空转）。代理恢复后我可以顺手把它也推上去。
